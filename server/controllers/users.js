var jwt = require('jsonwebtoken');
var decodedToken = '';
var verifyToken = require('../middleware/verifyToken');
module.exports = (req, res) => {

    return res.json(decodedToken.email);
    console.log(decodeToken);
    
    
    
}